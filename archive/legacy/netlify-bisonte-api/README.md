This directory contains the old duplicated Netlify functions that have been consolidated into /netlify/functions.

Files retained only for reference. They are excluded from deployment.

Safe to delete after verification of production endpoints.
